spring:
  application:
    name: other-tales-api

  # --- DATABASE CONFIG (Supabase PostgreSQL) ---
  datasource:
    url: jdbc:postgresql://db.gsslwdruiqtlztupekcd.supabase.co:5432/postgres?sslmode=require
    username: postgres
    password: sewweG-jeqwub-9bywny
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    show-sql: false
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    properties:
      hibernate:
        default_schema: public
        format_sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
    default-schema: public

  # --- SECURITY (OAuth2 Resource Server) ---
  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: https://gsslwdruiqtlztupekcd.supabase.co/auth/v1/.well-known/jwks.json

server:
  port: 8080
  error:
    include-message: always
    include-binding-errors: always

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html

logging:
  level:
    com.othertales: DEBUG
    org.springframework.security: DEBUG
